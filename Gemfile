source 'https://rubygems.org'

gem 'rails', '3.2.11'

gem 'sqlite3'

# assets (may be supplimented in production)
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
  gem 'therubyracer', :platforms => :ruby
end

group :development do
end

gem 'jquery-rails'
gem 'bcrypt-ruby', '~> 3.0.0', :require => 'bcrypt'
gem 'nested_form'
gem 'authlogic'
